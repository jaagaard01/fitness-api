projects:
  app:
    schemaPath: src/schema.graphql
    extensions:
      endpoints:
        default: http://localhost:4000
  prisma:
    schemaPath: generated/prisma-client/prisma.graphql
    extensions:
      prisma: api/prisma.yml